<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" android:maxSdkVersion="31" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.GiocoDel15">

        <activity
            android:name=".feature.nearby.NearbyTablesActivity"
            android:screenOrientation="unspecified" />

        <activity
            android:name=".feature.lobby.LobbyActivity"
            android:screenOrientation="unspecified" />

        <activity
            android:name=".feature.game.GameActivity"
            android:screenOrientation="unspecified" />

        <activity
            android:name=".feature.create.CreateTableActivity"
            android:screenOrientation="unspecified" />

        <activity
            android:name=".feature.home.HomeActivity"
            android:exported="true"
            android:screenOrientation="unspecified">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
